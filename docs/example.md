# Example Document

This is a starter document for the local RAG project.

The app uses Ollama to create embeddings for local files, stores those vectors
in a small JSON file, and retrieves relevant chunks when you ask a question.

You can replace this file with your own notes, documentation, PDFs converted to
text, meeting summaries, or knowledge base articles.
